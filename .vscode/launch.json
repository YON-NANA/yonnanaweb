{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "JavaScript デバッグ ターミナル",
            "request": "launch",
            "cwd": "${workspaceFolder}"
        }
    ]
}