# ウェブサイト更新マニュアル (GitHub編)

このウェブサイトは、専門的な知識がなくても、**GitHubの画面上で文字を書き換えるだけ**で更新できるように作られています。
主に以下の情報を更新できます：

1. **トップページのお知らせ・ニュース**
2. **里親募集中の犬・猫の情報**
3. **画像（写真）の追加・差し替え**

---

## 1. 準備：編集ページへアクセス

まず、以下のGitHubリポジトリ（データの保管場所）にアクセスし、ログインしてください。
**URL:** [https://github.com/YON-NANA/yonnanaweb](https://github.com/YON-NANA/yonnanaweb)

---

## 2. お知らせ（ニュース）の更新

トップページの「お知らせ」欄を更新する方法です。

1. ファイル一覧から **`content`** というフォルダをクリックします。
2. **`news.json`** というファイルをクリックします。
3. 画面右上の **鉛筆アイコン（Edit this file）** をクリックします。
4. 以下のようなデータが表示されますので、新しい情報を**一番上**に追加してください。

```json
{
  "items": [
    {
      "date": "2024.10.15",
      "category": "イベント",
      "title": "保護犬・保護猫譲渡会のお知らせ（徳島中央公園）",
      "url": "#"
    },
    ...
```

**【書き方の例】**

```json
    {
      "date": "2024.12.01",
      "category": "お知らせ",
      "title": "年末年始の休業について",
      "url": "#"
    },
```

※ **注意**: 項目ごとの区切りには `,` (カンマ) が必要です（最後の項目の後ろには不要）。

1. 編集が終わったら、画面右上の **「Commit changes...」** ボタンを押します。
2. 確認画面が出るので、そのまま **「Commit changes」** をもう一度押して保存完了です。
   → 数分後にウェブサイトに反映されます。

---

## 3. 里親募集情報の更新

里親募集中の犬・猫のデータを更新する方法です。

1. ファイル一覧から **`content`** フォルダをクリックします。
2. 犬の場合は **`dogs.json`**、猫の場合は **`cats.json`** をクリックします。
3. **鉛筆アイコン** をクリックして編集モードに入ります。
4. データを修正・追加・削除します。

**【データの見方】**

```json
    {
        "id": "dog01",          <-- ID（重複しない英数字なら何でもOK）
        "name": "ポチ",         <-- 名前
        "age": "3歳",           <-- 年齢
        "gender": "オス",       <-- 性別
        "image": "assets/images/dogs/pochi.jpg", <-- 写真のパス（後述）
        "status": "里親募集中",  <-- ステータス（募集中 / トライアル中 / 決定 など）
        "desc": "元気いっぱいの男の子です。" <-- 紹介文
    },
```

1. **「Commit changes」** で保存します。

---

## 4. 写真（画像）のアップロード

新しいワンちゃん・ネコちゃんの写真を載せる場合の手順です。

1. ファイル一覧から **`assets`** -> **`images`** と進みます。
2. 犬なら **`dogs`**、猫なら **`cats`** フォルダに入ります。
3. 画面右上の **「Add file」** ボタンから **「Upload files」** を選びます。
4. 手持ちの写真を画面にドラッグ＆ドロップします。
   ※ ファイル名は半角英数字にしてください（例: `pochi.jpg`）。日本語ファイル名は避けてください。
5. **「Commit changes」** で保存します。

**【JSONへの反映】**
アップロードした画像ファイル名を、先ほどの `dogs.json` や `cats.json` の `"image": "..."` 部分に書き込んでください。
例： `"image": "assets/images/dogs/pochi.jpg"`

---

## 5. 注意点

* **カンマ (,) の扱い**: JSONファイルでは、データの区切りにカンマが必須です。逆に、リストの最後の項目の後ろにカンマがあるとエラーになることがあります。
* **反映時間**: 保存してから実際のサイトに反映されるまで、通常 1〜3分程度かかります。ブラウザの再読み込みをして確認してください。
